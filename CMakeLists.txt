# minimum required version
cmake_minimum_required(VERSION 2.6)
# project name
PROJECT(UNIVERSE)
# illustrate message
MESSAGE(STATUS "Universe: a framework for server programing.")
# add subdirectories
ADD_SUBDIRECTORIES(me/sample)
